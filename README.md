# C - Simple Shell

## this simple shell project is a simple implementation of a UNIX command interpreter written in C. It imitates the behavior of the standard shell by handling user commands, executing programms via thier full paths, and managing the process lifecycle. 
